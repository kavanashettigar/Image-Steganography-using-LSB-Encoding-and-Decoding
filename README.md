# Image-Steganography-using-LSB-Encoding-and-Decoding
This project hides a secret text file inside a BMP image by encoding its size and content into the least significant bits of pixels. The image appears unchanged while carrying hidden data. During decoding, the hidden data is extracted to reconstruct the original text for secure transmission.
